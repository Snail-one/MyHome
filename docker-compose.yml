services:
  my-home:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      DATABASE_PATH: /app/data/my-home.sqlite
    volumes:
      - ./data:/app/data
    restart: unless-stopped
